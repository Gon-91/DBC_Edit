# 2026-01-10

## Done


### 2026-01-08

    - 모델에과 app menu action 관리를위한 command 패턴 적용 ("앱의 유즈케이스")
    - 동작 예시
    - QAction
      └─ Command (OpenFileCommand)
            └─ FileController
                  └─ Service (load_dbc_file)
                        └─ Model (DBCFile)
    - 목표는 UI는 진짜 시작적 요소 배치만 나머지 기능은 분리하기 위해 시작함
  -  --> context facade로 더 나눔

### 2026-01-06 완료
    - 모델 구조 만들기
      - 최상위 모델 (app)
        - 파일 모델 (file)
          - 메세지 모델 (messages)
            - 시그널 모델 (signals)

## Issue

## Next




1️⃣ 이 구조의 정체성 (이름부터 명확히)

이 구조는 다음의 혼합이지만 일관된 하나의 스타일입니다.

✔ 패턴 정체성
Command-driven MVC (Qt Desktop Style)


또는 더 정확히:

Command-based Application Layer + Facade Controller + Observable Model


❗ MVC / MVVM / Clean Architecture 중 하나가 아님
→ Desktop GUI 실무에서 매우 흔한 구조

2️⃣ 전체 계층 흐름 (최종 확정)
[ User Action ]
     ↓
QAction / Widget Signal
     ↓
UseCase (Command)
     ↓
Controller (Domain Facade)
     ↓
AppModel (State + Qt Signals)
     ↓
View (Dock / Widget) reacts


✔ View는 Model의 Signal만 구독
✔ UseCase는 Controller만 앎
✔ Controller는 Model + Service만 앎

3️⃣ 각 계층의 “단일 책임” 재정의
1. UseCase (Command)

역할

사용자 의도를 하나의 객체로 캡슐화

UI → Domain 진입점

흐름 제어만 담당

절대 하지 않는 것

파싱

비즈니스 규칙

View 업데이트

class OpenFileCommand(Command):
    def execute(self, parent):
        file_path = open_dialog(parent)
        self.file_controller.open_file(file_path)


📌 UseCase = 버튼 하나 / 메뉴 하나

2. Controller (Domain Facade)

역할

도메인 조작 API

Model을 “의미 있는 단위”로 변경

절대 하지 않는 것

UI

QFileDialog

QAction

class FileController:
    def open_file(self, path):
        dbc = load_dbc_file(path)
        self.model.add_file(dbc)


📌 Controller = Model에 대한 façade

3. Model (Observable State)

역할

앱 전체 상태 보유

변경 시 Signal emit

절대 하지 않는 것

UI

파일 다이얼로그

QAction

class AppModel(QObject):
    files_changed = Signal(list)
    current_file_changed = Signal(object)


📌 Qt에서는 Model이 QObject인 게 정상

4. View (Passive View)

역할

UI 표현

Model Signal → UI 반영

사용자 이벤트 → UseCase 호출

절대 하지 않는 것

Model 직접 조작

Service 호출

4️⃣ 이 흐름에 최적화된 디렉터리 구조 (권장)

아래는 지금까지 만든 코드 + 앞으로 확장 가능한 최종 구조입니다.

src/
│
├── main.py                    # 앱 진입점
│
├── app/
│   ├── app_context.py         # 객체 생성 / 의존성 조립
│   └── app_initializer.py     # QApplication + MainWindow
│
├── models/
│   ├── appmodel.py            # QObject 기반 상태 모델
│   └── domainmodels/
│       ├── dbc_file.py
│       ├── message.py
│       └── signal.py
│
├── controllers/
│   ├── file_controller.py
│   └── interfaces.py          # (선택) Protocol
│
├── services/
│   └── dbc_loader.py          # 순수 I/O + 파싱
│
├── usecases/
│   ├── base.py                # Command 인터페이스
│   └── file/
│       └── open_file.py
│
├── views/
│   ├── main_window.py
│   │
│   ├── menu/
│   │   └── menu_bar.py
│   │
│   ├── actions/
│   │   └── usecase_action.py  # QAction ↔ Command 어댑터
│   │
│   ├── docks/
│   │   ├── file_list_dock.py
│   │   ├── message_list_dock.py
│   │   └── signal_list_dock.py
│   │
│   └── widgets/
│       └── tables/
│           └── signal_table.py

5️⃣ AppContext의 역할 (중요)
❗ AppContext는 “God Object”가 아니다

하는 일

객체 생성

의존성 연결

소유권 관리

하지 않는 일

로직

상태 변경

UI

class AppContext:
    def __init__(self):
        self.model = AppModel()
        self.file_controller = FileController(self.model)
        self.usecases = UseCaseContext(self.file_controller)


📌 View는 Context를 모름
📌 MainWindow 생성 시 필요한 것만 주입

6️⃣ MainWindow는 무엇을 아는가?
class MainWindow(QMainWindow):
    def __init__(self, model, usecases):
        ...


✔ Model (Signal 구독용)
✔ UseCase (Action 연결용)

❌ Controller
❌ Service
❌ Context

7️⃣ 왜 이 구조가 “실무 안전한가”
장점 요약
항목	이유
확장성	메뉴 / 버튼 → UseCase 추가
테스트	Controller / Service 단독 테스트 가능
UI 변경	Model Signal만 유지하면 View 교체 가능
Qt 친화	QAction + Command 패턴 자연스러움
8️⃣ 이 구조에서 ViewModel이 없는 이유

👉 Model + Signal이 이미 ViewModel 역할을 수행

ViewModel은 다음 시점에만 도입하세요:

Table 정렬 / 필터링 로직 증가

동일 Model을 여러 View에 다르게 표현

계산/가공 로직이 View에 쌓이기 시작할 때

그 전까지는 과도한 계층입니다.

9️⃣ 최종 한 줄 정의 (당신 프로젝트)

“이 프로젝트는 Qt Desktop 앱을 위한
Command 기반 Application Layer + Facade Controller + Observable Model 구조다.”

다음으로 이어가면 좋은 주제:

✔ ViewModel 도입 기준 & 예제

✔ Signal 설계 원칙 (과다 emit 방지)

✔ 이 구조에서 테스트 작성법

✔ 계층 줄이는 리팩토링 포인트

어디로 갈까요?











***********  UI 관련 구조
views/
├─ main_window.py          # QMainWindow
│
├─ docks/                  # Dock 전용
│   ├─ explorer_dock.py    # File/Message 탭을 감싼 Dock
│   └─ signal_dock.py      # (추후) Signal Dock
│
├─ tabs/                   # QTabWidget 단위
│   ├─ file_tab.py
│   ├─ message_tab.py
│   └─ signal_tab.py
│
├─ widgets/                # 재사용 가능한 단일 위젯
│   ├─ file_list.py
│   ├─ message_table.py
│   ├─ signal_table.py
│   └─ signal_detail.py
│
├─ layouts/                # Composite Widget (선택)
│   └─ signal_layout.py
│
└─ actions/                # QAction / Command 바인딩
    └─ usecase_action.py